<!-- Call sidebar helper to pass to content_for :sidebar -->
<% sidebar(@category.sidebar) %>

<div class="category">
  <h2> <%= @category.title %></h2>
  <div class="description"><%= @category.description %></div>
</div>

<hr>

<h3>Related Posts</h3>
<%= render('posts/list', {posts: @category.posts}) %>
